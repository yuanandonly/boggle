
(* prints formatted top 10 scores. the input is a list of tuples where each 
  tuple is contains the player name, boggle score, and wordhunt score.*)
  (* FOR EMILY: just make there be like three columns with lines, format it
  how you think would look nice basically, can use Board.print_board as 
  inspiration*)
let print_leaderboard (info : (string * int * int) list) : unit = 
  failwith "unimplemented"